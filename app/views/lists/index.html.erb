<div class="container">
  <h1>Lists</h1>
  <%= link_to "Add", new_list_path, class: 'btn btn-primary my-3' %>
  <ol>
  <% @lists.each do |list| %>
    <li>
      <%= link_to list_path(list['id']) do %>
        <%= list.name %>
        <%= cl_image_tag(list.photo.key, width: 400, height: 300, crop: :fill) %>
      <% end %>
    </li>
  <% end %>

  </ol>
</div>

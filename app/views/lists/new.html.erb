<div class="container">
  <h1>Add to List</h1>

  <% if @list.errors.any? %>
    <% @list.errors.full_messages.each do |msg| %>
      <p><%= msg %></p>
    <% end %>
  <% end %>
  <%= render 'lists/form' %>
</div>
